Project: Lumina, a pedestrian/cyclist/runner route planner. Full spec in SPEC.md; status in PROGRESS.md.

Rules:

\- Phase 1 files (config.py, mock\_data.py, nlp.py, tests) are done. Don't modify them or rename fields/constants.

\- Work on one file or task per request. Finish or fix existing files instead of rewriting them.

\- Use the venv. Run pytest after changes and report results.

\- Never use the words "safest", "danger-free", "secure path" or "guarantee" in UI text.

\- Reply in 5 lines max unless asked. No long explanations.

